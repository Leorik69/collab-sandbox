<Window
    x:Class="NotifyIsland.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="Root" Background="Transparent">
        <Border
            x:Name="Pill"
            Width="168"
            Height="32"
            CornerRadius="16"
            BorderThickness="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            PointerPressed="Pill_PointerPressed"
            PointerEntered="Pill_PointerEntered"
            PointerExited="Pill_PointerExited"
            RightTapped="Pill_RightTapped">
            <Border.RenderTransform>
                <ScaleTransform x:Name="PillScale" ScaleX="1" ScaleY="1" CenterX="84" CenterY="16" />
            </Border.RenderTransform>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                <FontIcon x:Name="StateIcon" Glyph="&#xEA8F;" FontSize="14" Foreground="#E6E6EB" />
                <Border
                    x:Name="Badge"
                    MinWidth="16"
                    Height="16"
                    CornerRadius="8"
                    Padding="4,0"
                    Background="#E81123"
                    Visibility="Collapsed"
                    VerticalAlignment="Center">
                    <TextBlock
                        x:Name="BadgeText"
                        FontSize="10"
                        FontWeight="SemiBold"
                        Foreground="White"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Text="0" />
                </Border>
                <Ellipse
                    x:Name="Dot"
                    Width="8"
                    Height="8"
                    Fill="#66FFFFFF"
                    Opacity="1" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
